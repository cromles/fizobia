# Caddy — otomatik HTTPS (.tr dahil tüm domainler)
# Kurulum: sudo apt install caddy
# Kopyala: sudo cp deploy/Caddyfile.example /etc/caddy/Caddyfile
# hub.sizindomain.tr → kendi subdomain'iniz

hub.sizindomain.tr {
    reverse_proxy 127.0.0.1:8787
}
